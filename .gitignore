# --- Terraform ---
.terraform/
.terraform.lock.hcl.bak
*.tfstate
*.tfstate.*
*.tfvars
*.tfvars.json
*.tfplan
tfplan
crash.log
crash.*.log
.terraform.tfstate.lock.info

# --- Secrets / credentials ---
# Belt-and-braces: keys and kubeconfigs must never be committed.
ansible/files/
*.pem
*.key
id_rsa*
id_ed25519*
kubeconfig
*.kubeconfig
.env
.env.*

# --- Ansible ---
*.retry

# --- Local working notes / agent config (kept out of the repo) ---
docs/
CLAUDE.md

# --- Editor / OS noise ---
.vscode/
.idea/
*.swp
*~
.DS_Store
Thumbs.db
